<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mygroup5people.jxnufake.mapper.StuMapper">

    <sql id="stuColumns">
        id, username, password, name, gender, image, entrydate, stu_id, create_time, update_time
    </sql>

    <select id="getByUsernameAndPassword" resultType="com.mygroup5people.jxnufake.pojo.Stu">
        select
        <include refid="stuColumns"/>
        from jxnu.student
        where stu_id = #{stuId}
          and password = #{password}
    </select>

    <select id="getById" resultType="com.mygroup5people.jxnufake.pojo.Stu">
        select
        <include refid="stuColumns"/>
        from student
        where id = #{id}
    </select>

    <select id="getInfoById" resultType="com.mygroup5people.jxnufake.vo.StudentInfoVO">
        select s.id, s.username, s.name, s.gender, s.image, s.entrydate,
               s.stu_id, s.class_id, ac.class_name
        from student s
        join administrative_class ac on ac.id = s.class_id
        where s.id = #{id}
    </select>

    <update id="updateImageById">
        update jxnu.student
        set image = #{image}
        where id = #{id}
    </update>

    <update id="updatePassword">
        update student
        set password = #{newPassword}
        where id = #{id}
    </update>

</mapper>
